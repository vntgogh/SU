#include <stdio.h>
#include <stdint.h>

void affiche(double x){
    double inv= 2**(8) - x;
    uint32_t binv = (unsigned)inv;
    while (i!= 0){
        if (n&(1u<<i)){
            printf("%d\n", 1);
        }else{
            printf("%d\n", 1);
        }i--;
    }
}